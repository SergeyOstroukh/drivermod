# Быстрый старт с Supabase

## За 5 минут до первого запуска

### 1. Создайте проект Supabase (2 минуты)

1. Откройте https://supabase.com
2. Нажмите "Start your project" → "New Project"
3. Заполните:
   - Name: `Suppliers` (любое название)
   - Password: придумайте пароль (запомните!)
   - Region: выберите ближайший
   - Plan: **Free**
4. Нажмите "Create new project"
5. Подождите 1-2 минуты

### 2. Получите ключи (1 минута)

1. В проекте: **Settings** → **API**
2. Скопируйте:
   - **Project URL** (например: `https://xxxxx.supabase.co`)
   - **anon public** key (длинная строка)

### 3. Настройте приложение (1 минута)

Откройте `js/config.js` и вставьте ваши данные:

```javascript
window.SUPABASE_CONFIG = {
	url: 'https://ваш-проект.supabase.co',  // ← Вставьте сюда
	anonKey: 'eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...'  // ← Вставьте сюда
};
```

### 4. Создайте таблицу (1 минута)

1. В Supabase: **SQL Editor** → **New query**
2. Скопируйте весь код из файла `supabase/migrations/001_create_suppliers_table.sql`
3. Вставьте в редактор
4. Нажмите **Run** (или `Ctrl+Enter`)

### 5. Запустите приложение

Откройте `index.html` в браузере - готово! 🎉

Данные из `suppliers.json` импортируются автоматически при первом запуске.

---

## Нужна помощь?

См. подробную инструкцию: [SUPABASE_SETUP.md](SUPABASE_SETUP.md)

