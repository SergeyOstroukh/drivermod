# Сервис маршрутов к поставщикам

Веб-приложение для управления поставщиками и построения маршрутов с использованием Supabase (PostgreSQL).

## Возможности

- 📍 Просмотр списка поставщиков с координатами
- 🗺️ Построение маршрутов через Яндекс.Навигатор и Яндекс.Карты
- ➕ Добавление новых поставщиков через веб-интерфейс
- ✏️ Редактирование существующих поставщиков
- 🗑️ Удаление поставщиков
- 🔍 Поиск по названию
- 📦 Хранение данных в PostgreSQL через Supabase

## Варианты установки

Есть два способа работы с базой данных:

1. **Облачный Supabase** (рекомендуется для начала) - проще настроить
2. **Локальный Docker** - для разработки без интернета

---

## Вариант 1: Облачный Supabase (рекомендуется)

### Требования
- Аккаунт на [supabase.com](https://supabase.com) (бесплатный)
- Веб-браузер

### Быстрая настройка

1. **Создайте проект в Supabase** (см. подробную инструкцию в [SUPABASE_SETUP.md](SUPABASE_SETUP.md))
   - Зарегистрируйтесь на https://supabase.com
   - Создайте новый проект
   - Получите URL и API ключ

2. **Настройте конфигурацию**
   - Откройте `js/config.js`
   - Вставьте ваш Project URL и anon key

3. **Создайте таблицу**
   - В Supabase Dashboard → SQL Editor
   - Выполните SQL из `supabase/migrations/001_create_suppliers_table.sql`
   - Или используйте Table Editor (см. SUPABASE_SETUP.md)

4. **Откройте приложение**
   - Откройте `index.html` в браузере
   - Готово! 🎉

Подробная инструкция: [SUPABASE_SETUP.md](SUPABASE_SETUP.md)

---

## Вариант 2: Локальный Docker

### Требования
- Docker и Docker Compose
- Веб-браузер

### Установка и запуск

### 1. Запуск Docker контейнера с PostgreSQL

```bash
# Запустить все сервисы
docker-compose up -d

# Проверить статус
docker-compose ps

# Просмотр логов
docker-compose logs -f
```

После запуска будут доступны следующие сервисы:
- **PostgreSQL**: `localhost:5432`
- **PostgREST API**: `http://localhost:3000`

**Важно**: Для работы с локальным Docker нужно изменить `js/db.js` обратно на PostgREST API или использовать облачный Supabase.

### 2. Проверка базы данных

Миграции выполняются автоматически при первом запуске контейнера. Таблица `suppliers` будет создана автоматически.

Вы можете проверить это через подключение к PostgreSQL:
```bash
docker exec -it suppliers-postgres psql -U postgres -d postgres -c "SELECT * FROM suppliers LIMIT 5;"
```

Или проверить через REST API:
```bash
curl http://localhost:3000/suppliers
```

### 3. Запуск веб-приложения

Откройте файл `index.html` в браузере или используйте локальный веб-сервер:

```bash
# Python 3
python -m http.server 8000

# Node.js (если установлен http-server)
npx http-server

# PHP
php -S localhost:8000
```

Затем откройте `http://localhost:8000` в браузере.

## Структура проекта

```
.
├── docker-compose.yml          # Конфигурация Docker контейнеров
├── index.html                  # Главная страница
├── styles.css                  # Стили
├── suppliers.json              # Исходные данные (импортируются автоматически)
├── js/
│   ├── app.js                  # Основная логика приложения
│   └── db.js                   # Работа с Supabase/PostgreSQL
├── supabase/
│   ├── migrations/
│   │   └── 001_create_suppliers_table.sql  # SQL миграция
│   ├── kong.yml                # Конфигурация API Gateway
│   └── config.js               # Конфигурация Supabase
└── README.md                   # Этот файл
```

## Использование

### Добавление поставщика

1. Нажмите кнопку "Добавить" в шапке страницы
2. Заполните форму:
   - **Название** (обязательно)
   - **Адрес** (необязательно)
   - **Широта (lat)** (обязательно, например: 53.874443)
   - **Долгота (lon)** (обязательно, например: 27.413769)
   - **Дополнительная информация** (необязательно, JSON формат)
3. Нажмите "Сохранить"

### Редактирование поставщика

1. Нажмите кнопку редактирования (иконка карандаша) на карточке поставщика
2. Измените нужные поля
3. Нажмите "Сохранить"

### Удаление поставщика

1. Откройте редактирование поставщика
2. Нажмите кнопку "Удалить"
3. Подтвердите удаление

### Построение маршрута

1. Определите вашу геопозицию (нажмите "Обновить геопозицию")
2. Выберите поставщиков с помощью чекбоксов
3. Нажмите "Поехать по маршруту"
4. Выберите способ открытия маршрута (Яндекс.Навигатор или Яндекс.Карты)

## Управление Docker контейнерами

```bash
# Остановить контейнеры
docker-compose down

# Остановить и удалить данные (ОСТОРОЖНО: удалит все данные!)
docker-compose down -v

# Перезапустить контейнеры
docker-compose restart

# Просмотр логов
docker-compose logs -f postgres

# Подключение к PostgreSQL напрямую
docker exec -it suppliers-postgres psql -U postgres -d postgres
```

## Работа с базой данных

### Прямое подключение к PostgreSQL

```bash
docker exec -it suppliers-postgres psql -U postgres -d postgres
```

### SQL запросы

```sql
-- Просмотр всех поставщиков
SELECT * FROM suppliers;

-- Добавление поставщика
INSERT INTO suppliers (name, address, lat, lon) 
VALUES ('Новый поставщик', 'Адрес', 53.874443, 27.413769);

-- Обновление поставщика
UPDATE suppliers SET name = 'Новое название' WHERE id = 1;

-- Удаление поставщика
DELETE FROM suppliers WHERE id = 1;
```

## Решение проблем

### Ошибка подключения к базе данных

1. Убедитесь, что Docker контейнеры запущены:
   ```bash
   docker-compose ps
   ```

2. Проверьте логи:
   ```bash
   docker-compose logs postgres
   ```

3. Перезапустите контейнеры:
   ```bash
   docker-compose restart
   ```

### Таблица suppliers не найдена

Миграции должны выполняться автоматически. Если таблица не создана:

1. Проверьте, что файл миграции находится в `supabase/migrations/`
2. Пересоздайте контейнер:
   ```bash
   docker-compose down -v
   docker-compose up -d
   ```

### Порт уже занят

Если порты заняты, измените их в `docker-compose.yml`:

```yaml
ports:
  - "5432:5432"  # Измените первый порт на другой, если 5432 занят
```

## Технологии

- **Frontend**: Vanilla JavaScript, HTML5, CSS3
- **Backend**: PostgreSQL + PostgREST (REST API для PostgreSQL)
- **Containerization**: Docker, Docker Compose
- **Database**: PostgreSQL 15

## Лицензия

Свободное использование.

